import TodoList from "../components/TodoList";

const Home = () => {
  return (
    <div style={{display: "flex", justifyContent: "center", marginTop: "90px"}}>
      <TodoList />
    </div>
  );
};

export default Home;
